package Controllers;

import javafx.fxml.FXML;
import javafx.scene.control.ListView;

public class AllUploadedFileControllers {
    @FXML
    public ListView MyFilesList;

    // should show list of all uploaded files, which on clicking each list item it will open fileView on new window :;::
}
